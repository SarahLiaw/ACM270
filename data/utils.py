import numpy as np

def add_noise(data, noisy_percent):
    # if noisy percent is 0, then just return the data
    # if not, just make a copy of the noisy data based on 
    # the standard deviation of the data
    # and add noise to the data (ie take the std of the data and multiply it by the noisy percent)
    # and add that to the data
    # for each row in the data


def get_partial_observations(data, fraction_observed, seed=None):
    return "TODO"